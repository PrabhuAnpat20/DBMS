package mongoCon;
import com.mongodb.*;
import com.mongodb.client.MongoCollection;

import java.util.*;
import org.bson.*;
public class app {

	public static void main(String args[]) {
		System.out.println("hello");
		  MongoCollection col = new MongoClient(new MongoClientURI("mongodb://localhost:27017")).getDatabase("demo").getCollection("sppu");
		  Scanner sc  = new Scanner(System.in);
	}
}
